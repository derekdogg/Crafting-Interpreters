This is the basically the start of the implementation in the book crafting interpreters. 
It is up to the point of doing basic arithmetic.
Scanning has been started. That is a work in progress.

This code matches fairly closely the original c code in the book (as much as I can).
