// Passing non-closure values to callback natives.
// Each should produce a clean runtime error.

// Number instead of function
onKeyPressed(42); // expect runtime error: onKeyPressed() takes 1 function argument.
